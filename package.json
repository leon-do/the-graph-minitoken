{
  "name": "MiniToken",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ MiniToken",
    "create-local": "graph create --node http://localhost:8020/ MiniToken",
    "remove-local": "graph remove --node http://localhost:8020/ MiniToken",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 MiniToken"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.2",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
